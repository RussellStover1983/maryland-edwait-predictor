# Session / tooling state
.claude/
phase2-run.log
*.log

# Secrets (belt-and-suspenders — nested .gitignore also excludes these)
.env
.env.local
.env.*.local

# Raw HSCRC Excel drops — canonical copy lives at C:/dev/shared/data/hscrc/
volume-training-data/

# OS
.DS_Store
Thumbs.db
